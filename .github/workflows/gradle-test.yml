# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Run tests
'on':
  push:
    branches:
      - main
  
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./gradlew test
        env:
          DB_DATABASE: 'postgres'
          DB_HOST: 'db.mdrokateajssmkzwljii.supabase.co'
          DB_PORT: '5432'
